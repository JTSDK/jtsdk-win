JTSDK - Installers

These files are used to build installers. JTSDK downloads are from SVN
to perform updates. As such, these files and folder should not reside
within the Win32 directory as it would cause conflicts at initial checkout
without using the svn export --force command.
